<label for="{{forms.checkbox.name}}">
    <input class="input-chk"
            value="{{forms.checkbox.value}}"
            checked
            name="{{forms.checkbox.name}}"
            type="checkbox" />
    {{forms.checkbox.label}}
</label>
